<UserControl x:Class="RecruitAutomation.Browser.Controls.BrowserTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 导航按钮 -->
                <Button Grid.Column="0" Content="◀" Width="30" Height="28" 
                        Click="BtnBack_Click" ToolTip="后退"/>
                <Button Grid.Column="1" Content="▶" Width="30" Height="28" 
                        Margin="5,0,0,0" Click="BtnForward_Click" ToolTip="前进"/>
                <Button Grid.Column="2" x:Name="btnRefresh" Content="↻" Width="30" Height="28" 
                        Margin="5,0,0,0" Click="BtnRefresh_Click" ToolTip="刷新"/>
                <Button Grid.Column="3" Content="🏠" Width="30" Height="28" 
                        Margin="5,0,0,0" Click="BtnHome_Click" ToolTip="主页"/>

                <!-- 地址栏 -->
                <TextBox Grid.Column="4" x:Name="txtUrl" 
                         Margin="10,0" 
                         Padding="8,5"
                         VerticalContentAlignment="Center"
                         KeyDown="TxtUrl_KeyDown"/>

                <!-- 账号信息 -->
                <StackPanel Grid.Column="5" Orientation="Horizontal">
                    <Border Background="#E8F5E9" CornerRadius="3" Padding="8,4">
                        <TextBlock x:Name="txtAccountId" Text="账号: -" 
                                   FontSize="11" Foreground="#2E7D32"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 浏览器容器 -->
        <Grid Grid.Row="1" x:Name="browserContainer" Background="White"/>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="txtStatus" Grid.Column="0" 
                           Text="就绪" Foreground="#666" FontSize="11"/>
                <TextBlock x:Name="txtTitle" Grid.Column="1" 
                           Text="" Foreground="#999" FontSize="11"
                           TextTrimming="CharacterEllipsis"
                           HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
