<Window x:Class="RecruitAutomation.App.BrowserLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="账号登录" 
        Height="750" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 平台选择 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="选择平台:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cmbPlatform" Width="150" Padding="8,6" SelectionChanged="CmbPlatform_SelectionChanged">
                        <ComboBoxItem Content="BOSS直聘" Tag="https://www.zhipin.com/web/user/?ka=header-login" IsSelected="True"/>
                        <ComboBoxItem Content="智联招聘" Tag="https://passport.zhaopin.com/login"/>
                        <ComboBoxItem Content="前程无忧" Tag="https://login.51job.com/login.php"/>
                        <ComboBoxItem Content="猎聘" Tag="https://passport.liepin.com/"/>
                        <ComboBoxItem Content="58同城" Tag="https://passport.58.com/login"/>
                    </ComboBox>
                </StackPanel>

                <!-- 导航按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                    <Button x:Name="btnBack" Content="◀" Width="36" Height="30" Click="BtnBack_Click" 
                            ToolTip="后退" Margin="0,0,5,0"/>
                    <Button x:Name="btnForward" Content="▶" Width="36" Height="30" Click="BtnForward_Click" 
                            ToolTip="前进" Margin="0,0,5,0"/>
                    <Button x:Name="btnRefresh" Content="🔄" Width="36" Height="30" Click="BtnRefresh_Click" 
                            ToolTip="刷新"/>
                </StackPanel>

                <!-- 地址栏 -->
                <TextBox Grid.Column="2" x:Name="txtUrl" Margin="15,0" Padding="8,6" 
                         IsReadOnly="True" Background="#ECF0F1" FontSize="12"/>

                <!-- 状态指示 -->
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Ellipse x:Name="statusIndicator" Width="12" Height="12" Fill="#95A5A6" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtStatus" Text="就绪" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 浏览器容器 -->
        <Border Grid.Row="1" x:Name="browserContainer" Background="White" Margin="5">
            <TextBlock Text="正在加载浏览器..." HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="#95A5A6" FontSize="16"/>
        </Border>

        <!-- 底部操作栏 -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 账号名称输入 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="账号名称:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtAccountName" Width="200" Padding="8,6" 
                             Text="" Tag="输入账号备注名称"/>
                </StackPanel>

                <!-- 提示信息 -->
                <TextBlock Grid.Column="1" x:Name="txtTip" 
                           Text="💡 请在浏览器中完成登录，登录成功后点击「保存账号」" 
                           Foreground="#7F8C8D" VerticalAlignment="Center" 
                           HorizontalAlignment="Center" FontSize="13"/>

                <!-- 操作按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="取消" Padding="20,8" Margin="0,0,10,0" Click="BtnCancel_Click"/>
                    <Button x:Name="btnSave" Content="保存账号" Padding="20,8" 
                            Background="#27AE60" Foreground="White" BorderThickness="0"
                            Click="BtnSave_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
