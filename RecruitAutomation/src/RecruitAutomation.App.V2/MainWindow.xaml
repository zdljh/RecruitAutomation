<Window x:Class="RecruitAutomation.App.V2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="招聘自动化系统 V2" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#1E1E1E">
    
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3F3F46"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧导航栏 -->
        <Border Grid.Column="0" Background="#2D2D30">
            <StackPanel>
                <!-- Logo区域 -->
                <Border Background="#3F3F46" Padding="15">
                    <StackPanel>
                        <TextBlock Text="招聘自动化" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="App Shell: V2" Foreground="#4CAF50" FontSize="11" Margin="0,2,0,0"/>
                        <TextBlock Text="JOB-READER-REWRITE" Foreground="#FF9800" FontSize="10"/>
                    </StackPanel>
                </Border>
                
                <!-- 导航菜单 -->
                <StackPanel Margin="0,10,0,0">
                    <Button x:Name="btnNavBrowser" Content="🌐 浏览器管理" Style="{StaticResource NavButtonStyle}" Click="BtnNavBrowser_Click"/>
                    <Button x:Name="btnNavJobs" Content="📋 岗位管理" Style="{StaticResource NavButtonStyle}" Click="BtnNavJobs_Click" Background="#4CAF50"/>
                    <Button x:Name="btnNavCandidates" Content="👥 候选人库" Style="{StaticResource NavButtonStyle}" Click="BtnNavCandidates_Click"/>
                </StackPanel>
                
                <!-- 底部版本信息 -->
                <TextBlock Text="v2.0.0-debug" Foreground="#666" FontSize="10" Margin="15,20,0,0"/>
            </StackPanel>
        </Border>

        <!-- 右侧内容区 -->
        <Border Grid.Column="1" Background="#F5F5F5">
            <Grid x:Name="contentArea">
                <!-- 默认显示岗位管理页面 -->
            </Grid>
        </Border>
    </Grid>
</Window>
